spring:
  application:
    name: notification-service

  zipkin:
    service:
      name: notification-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/ecommerce  # MongoDB URI for connecting
notification:
  params:
    a: 1
    e: 3

server:
  port: 9002
